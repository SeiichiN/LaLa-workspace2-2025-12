<style>
.my-text {
background-color: #f5f5f5;
padding: 10px;
margin-left: 30%;
}
</style>

<div class="my-text">
Java21で、初心者向けに、Setを使ったサンプルプログラムを作れますか？
</div>

---

はい、できます！
**Java 21** で、**Set（重複しない集まり）**を使った、初心者向けの分かりやすいサンプルプログラムを紹介します。

---

# ✅ **Set を使った基本サンプル（HashSet）**

次は、**重複する値が自動的に取り除かれる**という Set の特徴を確認するプログラムです。

```java
import java.util.HashSet;
import java.util.Set;

public class SetSample {
    public static void main(String[] args) {

        // Set（HashSet）を作成
        Set<String> fruits = new HashSet<>();

        // 要素を追加
        fruits.add("Apple");
        fruits.add("Banana");
        fruits.add("Orange");
        fruits.add("Apple"); // ← 2回追加しても重複は入らない

        // Set の内容を表示
        System.out.println("Setの中身: " + fruits);

        // 要素が含まれているか確認
        System.out.println("Banana はある？: " + fruits.contains("Banana"));

        // 要素を削除
        fruits.remove("Orange");
        System.out.println("Orange を削除後: " + fruits);

        // 拡張for文で全要素をループ
        System.out.println("1つずつ表示:");
        for (String fruit : fruits) {
            System.out.println(" - " + fruit);
        }
    }
}
```

---

# 🔍 **実行結果（例）**

```
Setの中身: [Banana, Orange, Apple]
Banana はある？: true
Orange を削除後: [Banana, Apple]
1つずつ表示:
 - Banana
 - Apple
```

※ Set は順番が保証されないため、表示順は実行ごとに変わります。

---

# 📌 このサンプルで学べるポイント

* **Set は重複を許さない**
* HashSet は **順番がバラバラ**
* `add()` で要素追加
* `contains()` で存在チェック
* `remove()` で削除
* 拡張 for 文で中身を取り出せる

---

# ✨ おまけ：Set を使う定番の練習問題

例えばこんなことに Set はよく使われます：

* 入力された名前や数字の **重複チェック**
* ランダムに数字を生成して **重複なしの配列を作る**
* 単語のリストから **重複単語を削除して数える**

必要なら、これらのサンプルも作れます。

---

必要であれば、
🔹 TreeSet（並び順を自動で整える Set）
🔹 LinkedHashSet（順番を保持する Set）
のサンプルも作れます！

どうしましょう？
